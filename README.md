# workerpool
A production-ready Go worker pool for managing concurrent tasks with controlled resources: fixed workers, buffered queue, per-task timeouts, panic recovery, graceful shutdown, and metrics. Includes an article explaining worker pools in Go.
